<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLinearLayout" style="@style/ScrollViewLayout">
    <ExpandableListView
        android:id="@+id/explv_skills"
        android:layout_height="wrap_content"
        android:layout_width="match_parent" />

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/ScrollView">
    <LinearLayout android:id="@+id/linearLayout" style="@style/ScrollViewLayout">



        <ProgressBar
            style="@style/CustomProgressBarHorizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/progressbar"
            android:layout_gravity="center_horizontal"
            android:max="100"
            android:progress="50"
            android:indeterminate="false"
            />

        <ProgressBar
            android:id="@+id/progressbar3"
            style="@style/tallerBarStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:max="100"
            android:progress="50"
            android:indeterminate="false"
            />

        <fr.wetstein.mycv.view.TextProgressBar
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:id="@+id/progressbar2"
            android:layout_gravity="center_horizontal"
            android:max="100"
            android:progress="50"
            android:maxHeight="30dip"
            android:minHeight="30dip"
            />

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            >
            <ProgressBar
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/PROGRESS_BAR"
            />
            <TextView
                android:background="#00000000" android:text="Eclipse"
                android:layout_height="wrap_content"
                android:layout_width="wrap_content"
                android:layout_centerInParent="true"
            />
        </RelativeLayout>

    </LinearLayout>
</ScrollView>
</LinearLayout>